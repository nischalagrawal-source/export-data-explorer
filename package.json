{
  "name": "export-data-explorer",
  "version": "1.0.0",
  "description": "Indian Export Data Analysis System for tracking competitors and clients",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "node server/index.js",
    "client": "cd client && npm run dev",
    "install:all": "npm install && cd client && npm install",
    "build": "cd client && npm run build",
    "build:all": "cd client && npm install && npm run build",
    "build:prod": "npm install && cd client && npm install && npm run build",
    "start": "node server/index.js",
    "generate-sample": "node scripts/generate-sample-data.js"
  },
  "dependencies": {
    "@libsql/client": "^0.14.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "sql.js": "^1.10.2",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

